<%= form_for(@delivery_quest) do |f| %>
  <% if @delivery_quest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@delivery_quest.errors.count, "error") %> prohibited this delivery_quest from being saved:</h2>

      <ul>
      <% @delivery_quest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :reward %><br>
    <%= f.text_field :reward %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
