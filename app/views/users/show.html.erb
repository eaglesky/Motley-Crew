<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
    <section class="posted_quests">
      <a href="<%= new_delivery_quest_path %>" class="btn btn-primary">Post a new quest</a>
    </section>

    <section class="accepted_quests">
    </section>

  </aside>
</div>
